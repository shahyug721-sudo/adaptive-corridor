# Install into a venv on a SHORT path (e.g. C:\ecv). SUMO ships a deeply nested
# tools/ tree and the default Windows Store-Python site-packages prefix pushes
# it past the 260-character path limit, which fails the install with an OSError.
#
#   python -m venv C:\ecv
#   C:\ecv\Scripts\python.exe -m pip install -r requirements.txt

eclipse-sumo>=1.27
traci>=1.27
sumolib>=1.27

librosa>=0.11
soundfile>=0.13
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
xgboost>=3.0
matplotlib>=3.7
seaborn>=0.13
torch>=2.0
streamlit>=1.38
