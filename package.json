{
  "name": "adaptive-corridor",
  "version": "0.1.0",
  "description": "Dedicated emergency corridor with an adaptive ITS green zone, simulated on the Samruddhi Mahamarg.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node scripts/selftest.js",
    "experiment": "node scripts/experiment.js",
    "serve": "python -m http.server 8123 --directory web"
  },
  "engines": { "node": ">=18" }
}
